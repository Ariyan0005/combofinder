openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: ComboFinder API — phone display combo compatibility
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: brands
    description: Phone brands
  - name: models
    description: Phone models
  - name: combos
    description: Display combos
  - name: search
    description: Search
  - name: stats
    description: Statistics
  - name: public
    description: Public compatibility and technical reference pages
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /stats:
    get:
      operationId: getStats
      tags: [stats]
      summary: Get overall statistics
      responses:
        "200":
          description: Stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Stats"

  /search:
    get:
      operationId: searchModels
      tags: [search]
      summary: Search brands and models
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: brand_id
          in: query
          required: false
          schema:
            type: integer
        - name: category_id
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchResults"

  /brands:
    get:
      operationId: getBrands
      tags: [brands]
      summary: List all brands
      parameters:
        - name: category_id
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of brands
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Brand"
    post:
      operationId: createBrand
      tags: [brands]
      summary: Create a brand
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBrandInput"
      responses:
        "201":
          description: Created brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Brand"

  /brands/{id}:
    get:
      operationId: getBrand
      tags: [brands]
      summary: Get a brand by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Brand"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: updateBrand
      tags: [brands]
      summary: Update a brand
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBrandInput"
      responses:
        "200":
          description: Updated brand
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Brand"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteBrand
      tags: [brands]
      summary: Delete a brand
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /brands/{id}/models:
    get:
      operationId: getBrandModels
      tags: [brands]
      summary: Get all models for a brand
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Models
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Model"

  /models:
    get:
      operationId: getModels
      tags: [models]
      summary: List all models
      parameters:
        - name: brand_id
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of models
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Model"
    post:
      operationId: createModel
      tags: [models]
      summary: Create a model
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModelInput"
      responses:
        "201":
          description: Created model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Model"

  /models/{id}:
    get:
      operationId: getModel
      tags: [models]
      summary: Get a model by ID with its combos
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Model with combos
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ModelWithCombos"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: updateModel
      tags: [models]
      summary: Update a model
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModelInput"
      responses:
        "200":
          description: Updated model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Model"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteModel
      tags: [models]
      summary: Delete a model
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SuccessResponse"

  /models/{id}/combos:
    get:
      operationId: getModelCombos
      tags: [models]
      summary: Get all combos for a model
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Combos for the model
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Combo"

  /compatibilities/bulk:
    post:
      operationId: bulkCreateCompatibilities
      tags: [combos]
      summary: Bulk create compatibility entries for a model
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkCreateCompatibilitiesInput"
      responses:
        "201":
          description: Created combos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Combo"

  /public/compatibility/{brandSlug}/{modelSlug}:
    get:
      operationId: getPublicCompatibilityPage
      tags: [public]
      summary: Get a published compatibility page
      parameters:
        - name: brandSlug
          in: path
          required: true
          schema:
            type: string
        - name: modelSlug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Published compatibility and technical information
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicCompatibilityPage"
        "404":
          description: Page not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /public/isp/{slug}:
    get:
      operationId: getPublicIspPage
      tags: [public]
      summary: Get a published ISP record
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Published ISP record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicTechnicalPage"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /public/pinout/{slug}:
    get:
      operationId: getPublicPinoutPage
      tags: [public]
      summary: Get a published pinout record
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Published pinout record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicTechnicalPage"
        "404":
          description: Record not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /public/sitemap.xml:
    get:
      operationId: getPublicSitemap
      tags: [public]
      summary: Get the public technical-page sitemap
      responses:
        "200":
          description: XML sitemap
          content:
            application/xml:
              schema:
                type: string

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Stats:
      type: object
      properties:
        totalBrands:
          type: integer
        totalModels:
          type: integer
        totalCombos:
          type: integer
      required:
        - totalBrands
        - totalModels
        - totalCombos

    Brand:
      type: object
      properties:
        id:
          type: integer
        categoryId:
          type: integer
          nullable: true
        name:
          type: string
        logoUrl:
          type: string
          nullable: true
        modelCount:
          type: integer
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - name
        - modelCount
        - createdAt

    CreateBrandInput:
      type: object
      properties:
        categoryId:
          type: integer
          nullable: true
        name:
          type: string
        logoUrl:
          type: string
          nullable: true
      required:
        - name

    Model:
      type: object
      properties:
        id:
          type: integer
        brandId:
          type: integer
        brandName:
          type: string
        name:
          type: string
        releaseYear:
          type: integer
          nullable: true
        imageUrl:
          type: string
          nullable: true
        comboCount:
          type: integer
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - brandId
        - brandName
        - name
        - comboCount
        - createdAt

    ModelWithCombos:
      type: object
      properties:
        id:
          type: integer
        brandId:
          type: integer
        brandName:
          type: string
        name:
          type: string
        releaseYear:
          type: integer
          nullable: true
        imageUrl:
          type: string
          nullable: true
        combos:
          type: array
          items:
            $ref: "#/components/schemas/Combo"
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - brandId
        - brandName
        - name
        - combos
        - createdAt

    CreateModelInput:
      type: object
      properties:
        brandId:
          type: integer
        name:
          type: string
        releaseYear:
          type: integer
          nullable: true
        imageUrl:
          type: string
          nullable: true
      required:
        - brandId
        - name

    Combo:
      type: object
      properties:
        id:
          type: integer
        modelId:
          type: integer
        modelName:
          type: string
        brandName:
          type: string
        categoryId:
          type: integer
          nullable: true
        categoryName:
          type: string
          nullable: true
        name:
          type: string
        comboType:
          type: string
          enum: [OEM, Compatible, Refurbished]
        qualityGrade:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        partType:
          type: string
          nullable: true
        imageUrl:
          type: string
          nullable: true
        isPublished:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - modelId
        - modelName
        - brandName
        - name
        - comboType
        - createdAt

    Schematic:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        slug:
          type: string
          nullable: true
        modelId:
          type: integer
          nullable: true
        deviceBrand:
          type: string
          nullable: true
        deviceModel:
          type: string
          nullable: true
        schematicType:
          type: string
          nullable: true
        fileUrl:
          type: string
          nullable: true
        thumbnailUrl:
          type: string
          nullable: true
        fileSize:
          type: string
          nullable: true
        tags:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        component:
          type: string
          nullable: true
        pinNumber:
          type: string
          nullable: true
        pinName:
          type: string
          nullable: true
        voltage:
          type: string
          nullable: true
        ground:
          type: string
          nullable: true
        signalInfo:
          type: string
          nullable: true
        testPointInfo:
          type: string
          nullable: true
        isPublished:
          type: boolean
        createdAt:
          type: string
          format: date-time
      required: [id, title, isPublished, createdAt]

    SeoMetadata:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
      required: [title, description]

    PublicCompatibilityPage:
      type: object
      properties:
        model:
          $ref: "#/components/schemas/Model"
        compatibilities:
          type: array
          items:
            $ref: "#/components/schemas/Combo"
        technicalRecords:
          type: array
          items:
            $ref: "#/components/schemas/Schematic"
        canonicalPath:
          type: string
        canonicalUrl:
          type: string
        seo:
          $ref: "#/components/schemas/SeoMetadata"
      required: [model, compatibilities, technicalRecords, canonicalPath, canonicalUrl, seo]

    PublicTechnicalPage:
      type: object
      properties:
        record:
          $ref: "#/components/schemas/Schematic"
        canonicalPath:
          type: string
        canonicalUrl:
          type: string
        seo:
          $ref: "#/components/schemas/SeoMetadata"
      required: [record, canonicalPath, canonicalUrl, seo]

    BulkCreateCompatibilitiesInput:
      type: object
      properties:
        modelId:
          type: integer
        comboType:
          type: string
          enum: [OEM, Compatible, Refurbished]
        names:
          type: array
          items:
            type: string
      required:
        - modelId
        - comboType
        - names

    SearchResults:
      type: object
      properties:
        brands:
          type: array
          items:
            $ref: "#/components/schemas/Brand"
        models:
          type: array
          items:
            $ref: "#/components/schemas/Model"
        combos:
          type: array
          items:
            $ref: "#/components/schemas/Combo"
      required:
        - brands
        - models

    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
      required:
        - success

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
